{
  "name": "curator",
  "version": "0.0.1",
  "description": "Curation for web snippets",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd svelte-app && pnpm run dev\" \"cd electron-app && pnpm run dev\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
